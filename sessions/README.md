# Sessions

One directory per interview, named `<date>_<time>_<candidate>_<role>`. Each holds `session.json`,
the full machine-readable state, and `scorecard.md` once the interview is finished.

The tool creates this directory if it is missing. Commit the results you want to keep.
